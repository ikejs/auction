# Base URL of the DigitalOcean backend (REST API + socket.io).
# Local dev: http://localhost:4000   Production: https://api.auction.ike.dev
VITE_API_URL=http://localhost:4000

# Optional: shown in the admin's "public page" hint (backend reads this too).
VITE_PUBLIC_SITE_URL=http://localhost:3000
